# iOS 플랫폼 및 버전 지정
platform :ios, '16.4'

# Alamofire 버전 명시
target 'jumso' do
  use_frameworks!

  # Alamofire 5.10.2 버전 사용
  pod 'Alamofire', '5.10.2'

  # 테스트 타겟 설정 (필요 시)
  target 'jumsoTests' do
    inherit! :search_paths
  end

  target 'jumsoUITests' do
    inherit! :search_paths
  end
end

